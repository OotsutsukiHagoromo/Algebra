\section{Расчётная задача 6}

Направляющий вектор~$a \in \RR^3$ с координатами~$(1, 3, 1)^\top$ задаёт одномерное подпространство~$L_1 \leq \RR^3$, а линейное однородное уравнение
\[
	5x - 2y + 2z = 0
\]
--- двумерное подпространство~$L_2 \leq \RR^3$. Требуется:
\begin{enumerate}[1)]
	\item построить матрицы~$A$ проектирования на~$L_1$ параллельно~$L_2$ и~$B$ проектирования на~$L_2$ параллельно~$L_1$;
	\item доказать, что $AB = BA = 0$, $A^2 = A$, $B^2 = B$, $A + B = E$;
	\item найти ядро, образ, собственные числа и собственные векторы каждого оператора;
	\item выяснить, на какие компоненты распадается вектор~$b$ с координатами $(1, 1, 1)^\top$ при разложении~$\RR^3 = L_1 \oplus L_2$.
\end{enumerate}
\begin{proofm}
	Обозначим~$V := \RR^3$ и выберем базис~$\ee := (e_1, e_2, e_3) \subset V$. Пусть также~$V^*$ --- пространство, двойственное к~$V$, и~$\ee^* := (\eps^1, \eps^2, \eps^3)$ --- взаимный с~$\ee$ базис (это означает, что~$\eps^i(e_j) = \delta^i_j$).

	Напомню, что элементы~$\xi \in V^*$ --- это линейные формы, то есть штукенции вида
	\[
		\xi = \xi_1 \eps^1 + \xi_2 \eps^2 + \xi_3 \eps^3
		,
	\]
	где $\xi^\nu \in \RR$. В них можно засовывать вектор~$v \in V$: если $v = v^1 e_1 + v^2 e_2 + v^3 e_3$, то
	\[
		\xi(v) =
		\left( 
			\sum_{i = 1}^3
				\xi_i \eps^i
		\right)
		\left( 
			\sum_{j = 1}^3
				v^j e_j
		\right)
		=
		\sum_{i = 1}^3
		\sum_{j = 1}^3
			\xi_i v^j\,\eps^i(e_j)
		=
		\sum_{i = 1}^3
		\sum_{j = 1}^3
			\xi_i v^j\,\delta^i_j
		=
		\sum_{i = 1}^3
			\xi_i v^i
		.
	\]
	Обрати внимание, что в матричном виде это можно записать так:
	\[
		\xi(v) =
		\Vect{\xi_1 & \xi_2 & \xi_3}
		\Vect{v^1 \\ v^2 \\ v^3}
		.
	\]

	\begin{definition}
		\tdef{Аннулятор~$\Ann{\xi}$} линейной формы~$\xi$ --- это множество всех векторов, которые она зануляет:
		\[
			\Ann{\xi} :=
			\set{v \in V}{\xi(v) = 0}
			.
		\]
		Это множество образует подпространство.
	\end{definition}

	Тогда~$a \in V$ задаёт одномерное подпространство
	\[
		L_1 :=
		\opspan_\RR{a} :=
		\anspan{a} :=
		\set{ta}{t \in \RR},
	\]
	а однородное линейное уравнение~$\xi = 0$ задаёт подпространство~$L_2 := \Ann{\xi}$.

	Пусть теперь хотим составить проектор
	\[
		\pi_\nu \colon L_1 \oplus L_2 \to L_\nu
		.
	\]
	вдоль дополнительного к~$L_\nu$ подпространства~$L_{\hnu}$.
	
	Пусть теперь~$\uu = (u_1, u_2, u_3)$ --- базис в~$V$, такой, что
	\begin{align*}
	\end{align*}

	Если $v = v_1 + v_2$, $v_\nu \in L_\nu$, --- разложение вектора в~$L_1 \oplus L_2$, то
	\begin{align*}
		\pi_1(v) &:= v_1;
		\\
		\pi_2(v) &:= v_2.
	\end{align*}

	Поэтому в базисе~$\uu = (u_1, u_2)$ матрица оператора~$\pi_\nu$ имеет вид~$P_\nu$, где
	\begin{align*}
		P_1 &=
		\begin{pmatrix}
			1 & 0
			\\
			0 & 0
		\end{pmatrix}
		,
		&
		P_2 &=
		\begin{pmatrix}
			0 & 0
			\\
			0 & 1
		\end{pmatrix}
		.
	\end{align*}
	




\end{proofm}
